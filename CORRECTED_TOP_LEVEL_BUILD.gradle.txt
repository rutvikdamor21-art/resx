// This is the corrected, complete code for your TOP-LEVEL build.gradle file.
// Please replace the entire content of your file with this.

// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    ext.kotlin_version = '1.8.20'
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // NOTE: The incorrect Hibernate dependency has been removed from here.
        // This block is for build tool dependencies, like the Android Gradle Plugin.
        // Example: classpath 'com.android.tools.build:gradle:8.0.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
